import 'package:flutter/material.dart';

class Deliveries extends StatelessWidget {
  const Deliveries({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("deliveries"));
  }
}
